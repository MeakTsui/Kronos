# Cloudflare R2 configuration (example)
# Copy to .env (or export into your shell) and fill values.

# Required
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=

# Optional
# S3 endpoint; if empty will be derived as https://{R2_ACCOUNT_ID}.r2.cloudflarestorage.com
R2_ENDPOINT=
# Region is usually 'auto' for R2
R2_REGION=auto
# Object key prefix in the bucket
R2_PREFIX=predictions

# Optional client behavior toggles (not wired by default)
# Set to 1/true/yes/on if you want to call push_prediction_r2() instead of push_prediction()
# USE_R2_UPLOAD=1

# Existing service endpoints (examples)
# SERVER_BASE=http://127.0.0.1:8000
# CRICKET_API_BASE=https://app.cricket-ai.xyz
# CRICKET_API_KEY=YOUR_TOKEN
